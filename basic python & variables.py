# Basic python & variables

# Create a program that asks the user for their name and age
# Convert the age into months (assume 1 year = 12 months)

name = input("Enter name: ")
age = int(input("Enter age: "))

age *= 12

print(f"Hello {name}, you are {age} months old.")
